import pandas as pd
import matplotlib.pyplot as plt
import numpy as np

#load dataset to pandas dataframe
df=pd.readcsv("", encoding='latin1')

#plot dataset

#define x axis points
xpoints=df.iloc[:,0]

#define y axis points
ypoints=df.iloc[:,1]

#plot the x-y points
plt.plot(xpoints, ypoints)

#x-y axis labels
plt.xlabel("t")
plt.ylabel(r'$GDP_t$')

#show the plot figure
plt.show()

#moving average with 3 periods
F_t=df.GDPt.rolling(3,min_periods=1).mean()

#plot the x-y points
plt.plot(xpoints, ypoints, label = '$GDP_t$')
plt.plot(xpoints, y2points, label = '$F_t$')

#plot legend
plt.legend(loc="upper left")

#x-y axis labels
plt.xlabel("t")
#show the plot figure
plt.show()

#repport MSE&MAPE
#MAPE function

def mape(actual, pred):
    actual, pred = np.array(actual), np.array(pred)
    return np.mean(np.abs((actual-pred)/ actual))*100

#slice data with complete info (3 periods)
t=df.iloc[3:,0]
GDP_t = df.iloc[3: 1]
F_t=df.iloc[3:,2]

#report MSE
print("MSE: " , mean_squared_error(GDP_t, F_t))
#report MAPE
print("MAPE: " , round(mape(GDP_t, F_t),2))
